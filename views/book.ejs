<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>MyBookList</title>
</head>
<body>
  <%- include('partials/navbar.ejs') %>
  <div class="container">
    <br>
    <div class="text-center"><h2>MyBookList</h2></div>
    <br>
    <% if (book) { %> <!-- Check if data exists -->
      <div>
        <p><b>Book Name:</b><br> <%= book.name %></p>
        <p><b>Image:</b><br> <img src="<%= book.image %>" width="20%"></p>
        <p><b>Book Author:</b><br> <%= book.author %></p>
        <p><b>Book Publisher:</b><br> <%= book.publisher %></p>
      </div>
    <% } else { %>
      <p>No book found.</p>
    <% } %>
    <a href="/">Back</a>
  </div>
</body>
</html>
